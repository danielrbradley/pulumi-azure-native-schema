$token: azure-native:insights/v20220601:DataCollectionEndpointResponseMetricsIngestion
description: The endpoint used by clients to ingest metrics.
properties:
  endpoint:
    type: string
    description: The endpoint. This property is READ-ONLY.
type: object
required:
  - endpoint
