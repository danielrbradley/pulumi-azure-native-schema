$token: azure-native:insights/v20220601:PlatformTelemetryDataSource
description: Definition of platform telemetry data source configuration
properties:
  name:
    type: string
    description: "A friendly name for the data source. \r

      This name should be unique across all data sources (regardless of type) within the data collection rule."
  streams:
    type: array
    items:
      type: string
    description: List of platform telemetry streams to collect
type: object
required:
  - streams
