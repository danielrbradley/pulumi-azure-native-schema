$token: azure-native:insights:WindowsFirewallLogsDataSource
description: Enables Firewall logs to be collected by this data collection rule.
properties:
  name:
    type: string
    description: "A friendly name for the data source. \r

      This name should be unique across all data sources (regardless of type) within the data collection rule."
  streams:
    type: array
    items:
      type: string
    description: Firewall logs streams
type: object
required:
  - streams
