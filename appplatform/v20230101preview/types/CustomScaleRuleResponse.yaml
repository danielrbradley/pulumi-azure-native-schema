$token: azure-native:appplatform/v20230101preview:CustomScaleRuleResponse
description: Azure Spring Apps App Instance Custom scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:appplatform/v20230101preview:ScaleRuleAuthResponse"
    description: Authentication secrets for the custom scale rule.
  metadata:
    type: object
    additionalProperties:
      type: string
    description: Metadata properties to describe custom scale rule.
  type:
    type: string
    description: |-
      Type of the custom scale rule
      eg: azure-servicebus, redis etc.
type: object
