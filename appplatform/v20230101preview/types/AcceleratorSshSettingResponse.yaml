$token: azure-native:appplatform/v20230101preview:AcceleratorSshSettingResponse
description: Auth setting for SSH auth.
properties:
  authType:
    type: string
    description: |-
      The type of the auth setting.
      Expected value is 'SSH'.
    const: SSH
type: object
required:
  - authType
