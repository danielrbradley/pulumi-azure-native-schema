$token: azure-native:appplatform/v20200701:ConfigServerPropertiesResponse
description: Config server git properties payload
properties:
  configServer:
    type: object
    $ref: "#/types/azure-native:appplatform/v20200701:ConfigServerSettingsResponse"
    description: Settings of config server.
  error:
    type: object
    $ref: "#/types/azure-native:appplatform/v20200701:ErrorResponse"
    description: Error when apply config server settings.
  provisioningState:
    type: string
    description: State of the config server.
type: object
required:
  - provisioningState
