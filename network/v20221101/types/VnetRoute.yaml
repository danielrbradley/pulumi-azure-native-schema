$token: azure-native:network/v20221101:VnetRoute
description: List of routes that control routing from VirtualHub into a virtual network connection.
properties:
  staticRoutes:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20221101:StaticRoute"
    description: List of all Static Routes.
  staticRoutesConfig:
    type: object
    $ref: "#/types/azure-native:network/v20221101:StaticRoutesConfig"
    description: Configuration for static routes on this HubVnetConnection.
type: object
