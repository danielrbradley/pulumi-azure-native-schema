$token: azure-native:network/v20220901:PublicIPPrefixSku
description: SKU of a public IP prefix.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20220901:PublicIPPrefixSkuName"
    description: Name of a public IP prefix SKU.
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:network/v20220901:PublicIPPrefixSkuTier"
    description: Tier of a public IP prefix SKU.
type: object
