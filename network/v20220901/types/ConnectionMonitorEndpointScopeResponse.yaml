$token: azure-native:network/v20220901:ConnectionMonitorEndpointScopeResponse
description: Describes the connection monitor endpoint scope.
properties:
  exclude:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20220901:ConnectionMonitorEndpointScopeItemResponse"
    description: List of items which needs to be excluded from the endpoint scope.
  include:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:network/v20220901:ConnectionMonitorEndpointScopeItemResponse"
    description: List of items which needs to be included to the endpoint scope.
type: object
