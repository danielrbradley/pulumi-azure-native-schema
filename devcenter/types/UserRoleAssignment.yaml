$token: azure-native:devcenter:UserRoleAssignment
description: Mapping of user object ID to role assignments.
properties:
  roles:
    type: array
    items:
      type: string
    description: A map of roles to assign to the parent user.
type: object
