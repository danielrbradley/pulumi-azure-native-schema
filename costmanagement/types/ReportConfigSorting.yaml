$token: azure-native:costmanagement:ReportConfigSorting
description: The order by expression to be used in the report.
properties:
  direction:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement:ReportConfigSortingType"
    description: Direction of sort.
  name:
    type: string
    description: The name of the column to sort.
type: object
required:
  - name
