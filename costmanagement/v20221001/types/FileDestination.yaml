$token: azure-native:costmanagement/v20221001:FileDestination
description: Destination of the view data. This is optional. Currently only CSV format is supported.
properties:
  fileFormats:
    type: array
    items:
      oneOf:
        - type: string
        - $ref: "#/types/azure-native:costmanagement/v20221001:FileFormat"
    description: Destination of the view data. Currently only CSV format is supported.
type: object
