$token: azure-native:costmanagement/v20221005preview:ReportConfigAggregation
description: The aggregation expression to be used in the report.
properties:
  function:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:costmanagement/v20221005preview:FunctionType"
    description: The name of the aggregation function to use.
  name:
    type: string
    description: The name of the column to aggregate.
type: object
required:
  - function
  - name
