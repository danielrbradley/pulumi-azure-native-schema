$token: azure-native:labservices:SecurityProfile
description: The lab security profile.
properties:
  openAccess:
    $ref: "#/types/azure-native:labservices:EnableState"
    description: Whether any user or only specified users can register to a lab.
type: object
