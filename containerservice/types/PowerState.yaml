$token: azure-native:containerservice:PowerState
description: Describes the Power State of the cluster
properties:
  code:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice:Code"
    description: Tells whether the cluster is Running or Stopped
type: object
