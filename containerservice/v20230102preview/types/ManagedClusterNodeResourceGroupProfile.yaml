$token: azure-native:containerservice/v20230102preview:ManagedClusterNodeResourceGroupProfile
description: Node resource group lockdown profile for a managed cluster.
properties:
  restrictionLevel:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerservice/v20230102preview:RestrictionLevel"
    description: The restriction level applied to the cluster's node resource group
type: object
