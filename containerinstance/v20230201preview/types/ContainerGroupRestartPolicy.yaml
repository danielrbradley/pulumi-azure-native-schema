$token: azure-native:containerinstance/v20230201preview:ContainerGroupRestartPolicy
description: |
  Restart policy for all containers within the container group. 
  - `Always` Always restart
  - `OnFailure` Restart on failure
  - `Never` Never restart
type: string
enum:
  - value: Always
  - value: OnFailure
  - value: Never
