$token: azure-native:netapp/v20220901:SecurityStyle
description: The security style of volume, default unix, defaults to ntfs for dual protocol or CIFS protocol
type: string
enum:
  - value: ntfs
  - value: unix
