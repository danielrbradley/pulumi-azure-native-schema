$token: azure-native:servicefabric:ServiceEndpoint
description: The service endpoint properties.
properties:
  locations:
    type: array
    items:
      type: string
    description: A list of locations.
  service:
    type: string
    description: The type of the endpoint service.
type: object
required:
  - service
