$token: azure-native:azurestackhci:ClusterDesiredProperties
description: Desired properties of the cluster.
properties:
  diagnosticLevel:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:azurestackhci:DiagnosticLevel"
    description: Desired level of diagnostic data emitted by the cluster.
  windowsServerSubscription:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:azurestackhci:WindowsServerSubscription"
    description: Desired state of Windows Server Subscription.
type: object
