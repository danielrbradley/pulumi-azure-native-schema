$token: azure-native:storagecache:AmlFilesystemEncryptionSettingsResponse
description: AML file system encryption settings.
properties:
  keyEncryptionKey:
    type: object
    $ref: "#/types/azure-native:storagecache:KeyVaultKeyReferenceResponse"
    description: Specifies the location of the encryption key in Key Vault.
type: object
