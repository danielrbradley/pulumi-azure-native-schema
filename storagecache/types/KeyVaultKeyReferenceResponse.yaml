$token: azure-native:storagecache:KeyVaultKeyReferenceResponse
description: Describes a reference to key vault key.
properties:
  keyUrl:
    type: string
    description: The URL referencing a key encryption key in key vault.
  sourceVault:
    type: object
    $ref: "#/types/azure-native:storagecache:KeyVaultKeyReferenceResponseSourceVault"
    description: Describes a resource Id to source key vault.
type: object
required:
  - keyUrl
  - sourceVault
