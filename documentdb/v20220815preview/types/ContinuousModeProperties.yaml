$token: azure-native:documentdb/v20220815preview:ContinuousModeProperties
description: Configuration values for periodic mode backup
properties:
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:documentdb/v20220815preview:ContinuousTier"
    description: Enum to indicate type of Continuos backup mode
type: object
