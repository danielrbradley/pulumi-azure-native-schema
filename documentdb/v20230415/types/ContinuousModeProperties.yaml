$token: azure-native:documentdb/v20230415:ContinuousModeProperties
description: Configuration values for periodic mode backup
properties:
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:documentdb/v20230415:ContinuousTier"
    description: Enum to indicate type of Continuous backup mode
type: object
