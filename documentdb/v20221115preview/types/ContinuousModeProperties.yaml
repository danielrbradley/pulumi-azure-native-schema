$token: azure-native:documentdb/v20221115preview:ContinuousModeProperties
description: Configuration values for periodic mode backup
properties:
  tier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:documentdb/v20221115preview:ContinuousTier"
    description: Enum to indicate type of Continuos backup mode
type: object
