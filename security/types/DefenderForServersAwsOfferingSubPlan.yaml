$token: azure-native:security:DefenderForServersAwsOfferingSubPlan
description: configuration for the servers offering subPlan
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:security:SubPlan"
    description: The available sub plans
type: object
