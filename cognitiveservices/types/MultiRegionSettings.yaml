$token: azure-native:cognitiveservices:MultiRegionSettings
description: The multiregion settings Cognitive Services account.
properties:
  regions:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:cognitiveservices:RegionSetting"
  routingMethod:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:cognitiveservices:RoutingMethods"
    description: Multiregion routing methods.
type: object
