$token: azure-native:machinelearningservices/v20230201preview:QueueSettings
properties:
  jobTier:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20230201preview:JobTier"
    description: Enum to determine the job tier.
    default: Standard
  priority:
    type: integer
    description: Controls the priority of the job on a compute.
type: object
