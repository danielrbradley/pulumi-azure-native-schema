$token: azure-native:machinelearningservices/v20230401preview:CustomMetricThresholdResponse
properties:
  metric:
    type: string
    description: "[Required] The user-defined metric to calculate."
  threshold:
    type: object
    $ref: "#/types/azure-native:machinelearningservices/v20230401preview:MonitoringThresholdResponse"
    description: The threshold value. If null, a default value will be set depending on the selected metric.
type: object
required:
  - metric
