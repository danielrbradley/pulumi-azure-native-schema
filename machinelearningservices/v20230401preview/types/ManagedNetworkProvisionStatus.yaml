$token: azure-native:machinelearningservices/v20230401preview:ManagedNetworkProvisionStatus
description: Status of the Provisioning for the managed network of a machine learning workspace.
properties:
  sparkReady:
    type: boolean
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20230401preview:ManagedNetworkStatus"
    description: Status for the managed network of a machine learning workspace.
type: object
