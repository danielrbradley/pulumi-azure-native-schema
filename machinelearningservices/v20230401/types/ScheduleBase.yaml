$token: azure-native:machinelearningservices/v20230401:ScheduleBase
properties:
  id:
    type: string
    description: A system assigned id for the schedule.
  provisioningStatus:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20230401:ScheduleProvisioningState"
    description: The current deployment state of schedule.
  status:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:machinelearningservices/v20230401:ScheduleStatus"
    description: Is the schedule enabled or disabled?
type: object
