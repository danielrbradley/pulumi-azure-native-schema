$token: azure-native:machinelearningservices/v20230401:GridSamplingAlgorithm
description: Defines a Sampling Algorithm that exhaustively generates every value combination in the space
properties:
  samplingAlgorithmType:
    type: string
    description: |-
      
      Expected value is 'Grid'.
    const: Grid
type: object
required:
  - samplingAlgorithmType
