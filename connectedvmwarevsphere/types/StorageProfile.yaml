$token: azure-native:connectedvmwarevsphere:StorageProfile
description: Specifies the storage settings for the virtual machine disks.
properties:
  disks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:connectedvmwarevsphere:VirtualDisk"
    description: Gets or sets the list of virtual disks associated with the virtual machine.
type: object
