$token: azure-native:connectedvmwarevsphere/v20220715preview:StorageProfile
description: Specifies the storage settings for the virtual machine disks.
properties:
  disks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:connectedvmwarevsphere/v20220715preview:VirtualDisk"
    description: Gets or sets the list of virtual disks associated with the virtual machine.
type: object
