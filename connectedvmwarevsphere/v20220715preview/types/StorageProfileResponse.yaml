$token: azure-native:connectedvmwarevsphere/v20220715preview:StorageProfileResponse
description: Specifies the storage settings for the virtual machine disks.
properties:
  disks:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:connectedvmwarevsphere/v20220715preview:VirtualDiskResponse"
    description: Gets or sets the list of virtual disks associated with the virtual machine.
  scsiControllers:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:connectedvmwarevsphere/v20220715preview:VirtualSCSIControllerResponse"
    description: Gets or sets the list of virtual SCSI controllers associated with the virtual machine.
type: object
required:
  - scsiControllers
