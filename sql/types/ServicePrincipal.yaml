$token: azure-native:sql:ServicePrincipal
description: The managed instance's service principal configuration for a resource.
properties:
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:sql:ServicePrincipalType"
    description: Service principal type.
type: object
