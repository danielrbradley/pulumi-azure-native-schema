$token: azure-native:keyvault:LifetimeAction
properties:
  action:
    type: object
    $ref: "#/types/azure-native:keyvault:Action"
    description: The action of key rotation policy lifetimeAction.
  trigger:
    type: object
    $ref: "#/types/azure-native:keyvault:Trigger"
    description: The trigger of key rotation policy lifetimeAction.
type: object
