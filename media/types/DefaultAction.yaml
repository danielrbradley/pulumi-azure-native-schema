$token: azure-native:media:DefaultAction
description: The behavior for IP access control in Key Delivery.
type: string
enum:
  - description: All public IP addresses are allowed.
    value: Allow
  - description: Public IP addresses are blocked.
    value: Deny
