$token: azure-native:databoxedge/v20230101preview:SymmetricKeyResponse
description: Symmetric key for authentication.
properties:
  connectionString:
    type: object
    $ref: "#/types/azure-native:databoxedge/v20230101preview:AsymmetricEncryptedSecretResponse"
    description: Connection string based on the symmetric key.
type: object
