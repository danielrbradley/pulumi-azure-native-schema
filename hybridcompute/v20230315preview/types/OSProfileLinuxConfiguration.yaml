$token: azure-native:hybridcompute/v20230315preview:OSProfileLinuxConfiguration
description: Specifies the linux configuration for update management.
properties:
  assessmentMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20230315preview:AssessmentModeTypes"
    description: Specifies the assessment mode.
  patchMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20230315preview:PatchModeTypes"
    description: Specifies the patch mode.
type: object
