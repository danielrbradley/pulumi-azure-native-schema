$token: azure-native:hybridcompute/v20221227preview:OSProfileWindowsConfiguration
description: Specifies the windows configuration for update management.
properties:
  assessmentMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20221227preview:AssessmentModeTypes"
    description: Specifies the assessment mode.
  patchMode:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:hybridcompute/v20221227preview:PatchModeTypes"
    description: Specifies the patch mode.
type: object
