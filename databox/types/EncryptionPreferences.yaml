$token: azure-native:databox:EncryptionPreferences
description: Preferences related to the Encryption.
properties:
  doubleEncryption:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databox:DoubleEncryption"
    description: Defines secondary layer of software-based encryption enablement.
    default: Disabled
  hardwareEncryption:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:databox:HardwareEncryption"
    description: Defines Hardware level encryption (Only for disk)
type: object
