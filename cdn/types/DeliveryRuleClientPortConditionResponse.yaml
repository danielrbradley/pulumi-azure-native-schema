$token: azure-native:cdn:DeliveryRuleClientPortConditionResponse
description: Defines the ClientPort condition for the delivery rule.
properties:
  name:
    type: string
    description: |-
      The name of the condition for the delivery rule.
      Expected value is 'ClientPort'.
    const: ClientPort
  parameters:
    type: object
    $ref: "#/types/azure-native:cdn:ClientPortMatchConditionParametersResponse"
    description: Defines the parameters for the condition.
type: object
required:
  - name
  - parameters
