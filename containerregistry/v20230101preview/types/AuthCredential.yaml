$token: azure-native:containerregistry/v20230101preview:AuthCredential
description: Authentication credential stored for an upstream.
properties:
  name:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:containerregistry/v20230101preview:CredentialName"
    description: The name of the credential.
  passwordSecretIdentifier:
    type: string
    description: KeyVault Secret URI for accessing the password.
  usernameSecretIdentifier:
    type: string
    description: KeyVault Secret URI for accessing the username.
type: object
