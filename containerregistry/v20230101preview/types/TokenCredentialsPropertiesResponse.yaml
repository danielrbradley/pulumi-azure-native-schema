$token: azure-native:containerregistry/v20230101preview:TokenCredentialsPropertiesResponse
description: The properties of the credentials that can be used for authenticating the token.
properties:
  certificates:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20230101preview:TokenCertificateResponse"
  passwords:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:containerregistry/v20230101preview:TokenPasswordResponse"
type: object
