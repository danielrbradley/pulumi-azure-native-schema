$token: azure-native:authorization:ResourceSelectorResponse
description: The resource selector to filter policies by resource properties.
properties:
  name:
    type: string
    description: The name of the resource selector.
  selectors:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:authorization:SelectorResponse"
    description: The list of the selector expressions.
type: object
