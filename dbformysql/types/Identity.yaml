$token: azure-native:dbformysql:Identity
description: Properties to configure Identity for Bring your Own Keys
properties:
  type:
    $ref: "#/types/azure-native:dbformysql:ManagedServiceIdentityType"
    description: Type of managed service identity.
  userAssignedIdentities:
    type: array
    items:
      type: string
    description: Metadata of user assigned identity.
type: object
