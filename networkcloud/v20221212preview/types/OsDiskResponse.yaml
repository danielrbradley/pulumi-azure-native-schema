$token: azure-native:networkcloud/v20221212preview:OsDiskResponse
properties:
  createOption:
    type: string
    description: The strategy for creating the OS disk.
    default: Ephemeral
  deleteOption:
    type: string
    description: The strategy for deleting the OS disk.
    default: Delete
  diskSizeGB:
    type: number
    description: The size of the disk in gigabytes. Required if the createOption is Ephemeral.
type: object
required:
  - diskSizeGB
