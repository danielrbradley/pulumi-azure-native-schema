$token: azure-native:chaos:SimpleFilterParametersResponse
description: Model that represents the Simple filter parameters.
properties:
  zones:
    type: array
    items:
      type: string
    description: List of Azure availability zones to filter targets by.
type: object
