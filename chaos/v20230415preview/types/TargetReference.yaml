$token: azure-native:chaos/v20230415preview:TargetReference
description: Model that represents a reference to a Target in the selector.
properties:
  id:
    type: string
    description: String of the resource ID of a Target resource.
  type:
    oneOf:
      - type: string
      - $ref: "#/types/azure-native:chaos/v20230415preview:TargetReferenceType"
    description: Enum of the Target reference type.
type: object
required:
  - id
  - type
