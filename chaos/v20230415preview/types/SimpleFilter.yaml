$token: azure-native:chaos/v20230415preview:SimpleFilter
description: Model that represents a simple target filter.
properties:
  parameters:
    type: object
    $ref: "#/types/azure-native:chaos/v20230415preview:SimpleFilterParameters"
    description: Model that represents the Simple filter parameters.
  type:
    type: string
    description: |-
      Enum that discriminates between filter types. Currently only `Simple` type is supported.
      Expected value is 'Simple'.
    const: Simple
type: object
required:
  - type
