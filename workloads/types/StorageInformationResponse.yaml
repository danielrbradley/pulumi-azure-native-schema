$token: azure-native:workloads:StorageInformationResponse
description: "Storage details of all the Storage accounts attached to the VM. For e.g. NFS on AFS Shared Storage. "
properties:
  id:
    type: string
type: object
required:
  - id
