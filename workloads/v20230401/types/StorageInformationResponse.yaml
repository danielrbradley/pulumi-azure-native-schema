$token: azure-native:workloads/v20230401:StorageInformationResponse
description: "Storage details of all the Storage accounts attached to the VM. For e.g. NFS on AFS Shared Storage. "
properties:
  id:
    type: string
type: object
required:
  - id
