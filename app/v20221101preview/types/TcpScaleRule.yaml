$token: azure-native:app/v20221101preview:TcpScaleRule
description: Container App container Tcp scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:app/v20221101preview:ScaleRuleAuth"
    description: Authentication secrets for the tcp scale rule.
  metadata:
    type: object
    additionalProperties:
      type: string
    description: Metadata properties to describe tcp scale rule.
type: object
