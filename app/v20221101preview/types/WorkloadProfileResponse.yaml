$token: azure-native:app/v20221101preview:WorkloadProfileResponse
description: Workload profile to scope container app execution.
properties:
  maximumCount:
    type: integer
    description: The maximum capacity.
  minimumCount:
    type: integer
    description: The minimum capacity.
  name:
    type: string
    description: Workload profile type for the workloads to run on.
  workloadProfileType:
    type: string
    description: Workload profile type for the workloads to run on.
type: object
required:
  - name
  - workloadProfileType
