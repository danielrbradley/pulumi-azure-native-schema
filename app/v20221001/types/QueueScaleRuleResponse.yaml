$token: azure-native:app/v20221001:QueueScaleRuleResponse
description: Container App container Azure Queue based scaling rule.
properties:
  auth:
    type: array
    items:
      type: object
      $ref: "#/types/azure-native:app/v20221001:ScaleRuleAuthResponse"
    description: Authentication secrets for the queue scale rule.
  queueLength:
    type: integer
    description: Queue length.
  queueName:
    type: string
    description: Queue name.
type: object
