$token: azure-native:recoveryservices:WeeklySchedule
properties:
  scheduleRunDays:
    type: array
    items:
      $ref: "#/types/azure-native:recoveryservices:DayOfWeek"
  scheduleRunTimes:
    type: array
    items:
      type: string
    description: List of times of day this schedule has to be run.
type: object
