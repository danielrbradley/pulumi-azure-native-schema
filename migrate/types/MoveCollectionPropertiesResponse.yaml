$token: azure-native:migrate:MoveCollectionPropertiesResponse
description: Defines the move collection properties.
properties:
  errors:
    type: object
    $ref: "#/types/azure-native:migrate:MoveCollectionPropertiesResponseErrors"
    description: Defines the move collection errors.
  provisioningState:
    type: string
    description: Defines the provisioning states.
  sourceRegion:
    type: string
    description: Gets or sets the source region.
  targetRegion:
    type: string
    description: Gets or sets the target region.
  version:
    type: string
    description: Gets or sets the version of move collection.
type: object
required:
  - errors
  - provisioningState
  - sourceRegion
  - targetRegion
